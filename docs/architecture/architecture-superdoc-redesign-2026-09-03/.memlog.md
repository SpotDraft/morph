---
scope: SpotDraft SuperDoc document-editor service (this repo) plus its two known consumers — justitia-agent REST tools and angular-frontend sidebar SuperDoc collaboration
purpose: build-substrate plus isolated-mode implementation in morph/
altitude: initiative
project: supereditor-service
created: 2026-09-03
updated: 2026-09-04
reviewer_gate: 2026-09-03
old_spec_reconcile: 2026-09-04
intent: create
working_mode: headless-fast-path
---
- (constraint) Cloud Run exposes one port; REST and collaboration already share Fastify and PORT. Do not reintroduce a second listen port or host-process cleanup at boot.
- (constraint) justitia-agent treats HTTP 404 as session-expired, retries only 429/503, and never treats paragraph-edit content failures as 404. That status contract is frozen.
- (constraint) justitia-agent still names this service SUPEREDITOR and calls upload/search/get-content/replace/replace-all/insert-content/add-comment/get-comments/export plus paragraph-scoped endpoints.
- (constraint) angular-frontend sidebar-ai resolves region-specific superdocSocketUrls ending in /collaboration and passes them into docx-viewer. SuperDoc v2 rooms use a different document format than v1; a v2 editor must not join a v1 room.
- (constraint) Node 20 is the pinned runtime (.nvmrc). Chainguard node-fips:20 is the container base. instrumentation.ts must load before Fastify boots.
- (constraint) SUPERDOC_PUBLIC_LICENSE_KEY is required at process start today; keep a SuperDoc license identity for the new host.
- (assumption, superseded 2026-09-04) The five named source specs were first reconstructed from filenames. They are now in the repo root and were read in full.
- (assumption) Headless cloud-agent run: coaching path skipped; inferences tagged here; Fast path with assumption tags.
- (assumption) User asked for architecture redesign only — no implementation, no justitia-agent or angular-frontend code in this PR.
- (direction) Redesign the whole host around SuperDoc's newer code-based Document API rather than extending the JSDOM + ProseMirror-offset editor.
- (version) Verified 2026-09-03 on npm: @superdoc/sdk 2.8.0 (CLI-backed Node Document API; published 2026-09-01), @superdoc/cli 0.31.0, superdoc 2.11.0 (browser v2 engine), @hocuspocus/server 4.6.0, @harbour-enterprises/superdoc 1.46.3 (current v1 line; this repo is on ^1.46.2), fastify 5.12.1 latest / 5.8.5 in repo, yjs 13.6.32, Node 20.
- (constraint) SuperDoc docs forbid importing @superdoc/headless or @superdoc/document-api-v2-adapter as customer surfaces. Official Node integration is @superdoc/sdk. Official browser integration is the unscoped superdoc package.
- (constraint) SuperDoc safety docs: consequential agent edits use changeMode tracked; save to a separate output; close sessions on every path; attribute an explicit user; inspect receipts; do not log document contents; bound stale-revision retries to one re-query.
- (decision) AD-1 Document-engine command host. Binds all. Prevents a second home-grown editor/sync stack. Rule: SuperDoc Document API is the only mutation authority; HTTP and collaboration are adapters.
- (decision) AD-2 Official SDK only. Binds hosts/documents. Prevents JSDOM Editor internals and custom ProseMirror transactions. Rule: all reads/writes go through @superdoc/sdk document handles; no editor.state / editor.dispatch / trackInsert mark fabrication.
- (decision) AD-3 Address by query, not offsets. Binds agent tools and routes. Prevents ProseMirror from/to as the canonical targeting model. Rule: locate with query.match or extract/block addresses; bind expectedRevision; re-query after any mutation.
- (decision) AD-4 Plans for multi-edit. Binds multi-step agent turns. Prevents partial multi-edit application. Rule: two or more dependent edits use mutations.preview then mutations.apply with atomic true; single independent edits may use the direct operation.
- (decision) AD-5 Tracked by default for meaning changes. Binds agent mutations. Prevents silent direct rewrites of contract language. Rule: changeMode tracked unless the caller explicitly requests direct for a mechanical edit.
- (decision) AD-6 Handle lifecycle. Binds sessions. Prevents immortal in-process editors. Rule: open, inspect, mutate, inspect receipt, persist last-good DOCX, close on every path; warm handles are a cache, not the durable store.
- (decision) AD-7 Process isolation. Binds runtime/memory. Prevents JSDOM SuperDoc from living in the Fastify heap. Rule: the API process hosts orchestration; the engine runs behind the SDK/CLI process boundary.
- (decision) AD-8 Isolated vs shared access. Binds documents and collaboration. Prevents custom PM-to-Yjs dual-write. Rule: agent-only sessions open a persisted DOCX; human-visible sessions open the same v2 collaboration room the browser joins.
- (decision) AD-9 V2 collaboration rooms. Binds collaboration and frontend rollout. Prevents v1 SuperDocCollaboration rooms. Rule: new rooms are SuperDoc v2 (Hocuspocus or y-websocket); v1 Yjs format is retired with the JSDOM editor.
- (decision) AD-10 Last-good DOCX is durable. Binds persistence and export. Prevents Redis YDoc snapshots from being the only recoverability. Rule: persist a last-good DOCX after successful mutations; export writes a distinct artifact; do not treat export as proof the source was overwritten.
- (decision) AD-11 This service stays the agent HTTP boundary. Binds routes and justitia-agent. Prevents splitting upload/edit/export across ad-hoc SDK calls in the agent without a host contract. Rule: justitia-agent keeps talking HTTP here; new routes expose Document API shapes; old position routes become a compatibility facade.
- (decision) AD-12 Explicit author identity. Binds comments and tracked changes. Prevents generic CLI authorship. Rule: every open() carries the session user; missing user is a 400, not a default CLI author.
- (decision) AD-13 Receipts and one stale retry. Binds all mutations. Prevents silent success and unbounded retries. Rule: callers inspect receipt.success; REVISION_MISMATCH family may re-query and retry once; never drop expectedRevision to force a pass.
- (decision) AD-14 Safe telemetry. Binds analytics and logs. Prevents clause text in logs. Rule: log operation name, session id, failure code, receipt status; never document text, full payloads, or complete receipts.
- (decision) AD-15 Single-port Cloud Run host. Binds server and collaboration transport. Prevents a second public port. Rule: Fastify remains the public listener; collaboration websocket shares PORT.
- (decision) AD-16 Compat then delete. Binds migration. Prevents a permanent dual editor stack. Rule: v1 JSDOM helpers and custom replace/insert are removed once the Document API routes and shared-room path are the only writers; from/to targeting is deprecated, not reimplemented as the long-term model.
- (decision) AD-17 Status-code contract frozen. Binds HTTP errors. Prevents justitia-agent from treating content failures as expiry. Rule: unknown/expired session is 404; target/content/validation failures are 400; capacity is 503 with Retry-After.
- (deferred) Liveblocks as a collaboration provider — not needed while we operate our own room server.
- (deferred) Embedding SuperDoc MCP or createAgentToolkit inside this host — the agent already has tools; this host stays a deterministic Document API service.
- (deferred) Calling Python superdoc-sdk from justitia-agent and bypassing this service — would drop shared-room, analytics, and session contracts.
- (deferred) Content-control / template-first authoring as the default edit style — Document API supports it; current agent loop is still query-and-rewrite.
- (deferred) Productized version history and accept/reject-by-agent — humans remain the review authority; agent toolkits should exclude accept/reject by default.
- (question) What is the measured CLI-process RSS for representative SpotDraft contracts, and how many concurrent warm handles fit the current 8 GiB Cloud Run budget?
- (question) When will angular-frontend ship SuperDoc v2 + v2Collaboration, which gates shared-room mode?
- (question) Should the compatibility facade attempt a lossy projection-source-map for legacy from/to, or fail closed and force paragraph/query tools?
- (event) Architecture workspace bound at docs/architecture/architecture-superdoc-redesign-2026-09-03
- (event) Reviewer gate: lint_spine.py ok; rubric concerns; version-reality fail on Hocuspocus 4.6.0 vs Node 20; adversarial Alpha/Beta fork; reconcile gaps on roomMode, /collaboration path, 429, license.
- (version) Re-check: SuperDoc v2 example pins @hocuspocus/server ^2.13.6 (lock 2.15.3) and @hocuspocus/provider ^2.13.6. Hocuspocus 4.6.0 requires Node >=22. @superdoc/sdk 2.8.0 uses platform binaries, not npm @superdoc/cli. 2.8.0 types use roomMode, not onMissing.
- (decision) AD-9 amended: pin Hocuspocus 2.15.3 / provider 2.13.6; documentId equals sessionId; public path /collaboration with ?room= alias; no v2 rooms until sidebar v2; SDK shared open is roomMode join.
- (decision) AD-6/AD-10/AD-21 amended: isolated recovers last-good only; shared live body is the room; last-good is a documents-owned snapshot; persist failure is 503; export does not delete the session.
- (decision) AD-3 amended: fail closed on from/to and position; require exactlyOne for single-clause writes; find is not a write locator.
- (decision) AD-18 through AD-23 added: session authority, session record, promoteToShared, shared flush, one admission function, required license key.
- (decision) AD-12 is a breaking change vs today's anonymous upload default.
- (decision) AD-13 retry owner is documents only; HTTP 200 requires receipt plus persist.
- (assumption) @superdoc/sdk-linux-x64 on Chainguard node-fips:20 is unverified and deferred as a step-1 gate.
- (question) superseded: facade from/to source-map vs fail-closed — decided fail-closed.
- (event) Spine re-distilled after reviewer gate; lint_spine.py ok
- (event) 2026-09-04: old specs pulled onto this branch and read in full (document_editor_redesign.md, document-editor-redesign-spec.md, document_editor_plan.txt, three LLD phases).
- (correction) First-run Phase 3 reconstruction was wrong. The LLD is defined-term consistency, cross-reference drift, and the numbering ripple — not REST-versus-Yjs. Dual-write remains AD-8 / AD-21.
- (decision) Keep SuperDoc Document API / @superdoc/sdk as the write path. Do not revive DocumentEditingPort + SuperdocLocalAdapter, ProseMirror JSON at rest, host-minted cell blockIds, or raw OOXML editing.
- (decision) Commercial SuperDoc partnership covers AGPL (later redesign.md / 2026-07-05 plan update). Remaining SDK gates are fidelity and memory, not license. Distinct from AD-23 process license key.
- (decision) AD-1 / AD-3 / AD-4 / AD-7 / AD-10 / AD-13 tightened from the specs: no raw XML; insert-at-top is structural before; agent never types list prefixes; preview is invisible; recycle workers on memory/idle; not zero DOM; no global window in Fastify; export field refresh; typed failure codes.
- (decision) AD-24 compact outline with engine numbering labels; tables/footnotes/content-controls visible; full text on demand.
- (decision) AD-25 bulk change is a visible match set plus exclusions, never a bare count.
- (decision) AD-26 expectedText / expectedContext sits beside expectedRevision.
- (decision) AD-27 preview never persists or leaks to the room; human approval later reuses the same payload.
- (decision) AD-28 host consistency index after isolated Document API cutover. Terms patch on touched blocks; refs re-resolve globally after structural edits; flag drift, do not auto-fix.
- (decision) Phase 0 on the current JSDOM stack is optional tactical work, not a second target architecture.
- (question) Does SuperDoc export refresh REF / number fields or only emit them dirty?
- (question) Does extract / info already carry computed numbering labels, or must the host call lists.get per item?
- (event) 2026-09-04: wrote IMPLEMENTATION-HANDOFF.md for the next chat — wave 1 isolated writer, parallel sub-agents after hosts/documents substrate, SuperDoc docs as engine reference.
- (event) 2026-09-04: isolated writer implementation started in morph/. SDK spike on @superdoc/sdk 2.8.0 succeeded (open, query.match, tracked replace, insert-before heading, distinct save). Host uses nested receipt unwrap; expectedRevision retry uses live info().revision then open revision. v2 rooms still not created.
- (event) 2026-09-04: measured host+engine RSS. 2 GiB holds ~80–100 Ping-MSA warm docs; write slots default 8. closeHandle does not free native RSS; engine recycle on last close.
- (decision) Hydration stays sessionful. Redis (or file/memory) holds meta + last-good only. Warm handle is a cache. Not a stateless per-request editor. Redis required for Cloud Run / multi-instance.
- (decision) Agent experience: inspect-first, typed failures with nextAction, no bare 500, unknown routes are 400 UNKNOWN_ROUTE. Table/format/list/heading tools added. Collaboration v2 still not performing.

